{
  "name": "nmos-pocketrisu-plugin",
  "version": "0.1.0-beta.11",
  "private": true,
  "type": "module",
  "description": "NMOS thin host adapter for PocketRisu (V3 plugin)",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
