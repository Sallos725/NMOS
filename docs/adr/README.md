# Architecture Decision Records

Only create an ADR when implementation requires a non-obvious decision that is not already settled
by `ARCHITECTURE.md`.

Naming:

```text
0001-short-title.md
0002-short-title.md
```

Do not use ADRs to bypass open owner decisions.
